//First checkbox groups with name of aptype
function selectOnlyThis(id) {
    var myCheckbox = document.getElementsByName("aptype");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//second checkbox group with name of apinfo
function selectOnlyThis1(id) {
    var myCheckbox = document.getElementsByName("apinfo");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//third checkbox group with name of conflictofinterest
function selectOnlyThis2(id) {
    var myCheckbox = document.getElementsByName("conflictofinterest");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//fourth checkbox group with name of humansubjects
function selectOnlyThis3(id) {
    var myCheckbox = document.getElementsByName("humansubjects");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//fifth checkbox group with name of animals
function selectOnlyThis4(id) {
    var myCheckbox = document.getElementsByName("animals");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//sixth checkbox group with name of radioisotopes
function selectOnlyThis5(id) {
    var myCheckbox = document.getElementsByName("radioisotopes");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//Seventh checkbox group with name of biohazards
function selectOnlyThis6(id) {
    var myCheckbox = document.getElementsByName("biohazards");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//Eighth checkbox group with name of clearance
function selectOnlyThis7(id) {
    var myCheckbox = document.getElementsByName("clearance");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//Nineth checkbox group with name of masenoinclusion
function selectOnlyThis8(id) {
    var myCheckbox = document.getElementsByName("masenoinclusion");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//tenth checkbox group with name of priority
function selectOnlyThis9(id) {
    var myCheckbox = document.getElementsByName("priority");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//Eleventh checkbox group with name of conceptualframeworkclarity
function selectOnlyThis10(id) {
    var myCheckbox = document.getElementsByName("conceptualframeworkclarity");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//twelveth checkbox group with name of researchquestionclarity
function selectOnlyThis11(id) {
    var myCheckbox = document.getElementsByName("researchquestionclarity");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//thirteenth checkbox group with name of objectivequestionclarity
function selectOnlyThis12(id) {
    var myCheckbox = document.getElementsByName("objectivequestionclarity");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//Fourteenth checkbox group with name of studyproceduresclarity
function selectOnlyThis13(id) {
    var myCheckbox = document.getElementsByName("studyproceduresclarity");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//Fifteenth checkbox group with name of potentialbiasesaddressing
function selectOnlyThis14(id) {
    var myCheckbox = document.getElementsByName("potentialbiasesaddressing");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//Sixteenth checkbox group with name of communityrelevance
function selectOnlyThis15(id) {
    var myCheckbox = document.getElementsByName("communityrelevance");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//seventeenth checkbox group with name of animaluse
function selectOnlyThis16(id) {
    var myCheckbox = document.getElementsByName("animaluse");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//Eighteenth checkbox group with name of biologicalmaterialstorage
function selectOnlyThis17(id) {
    var myCheckbox = document.getElementsByName("biologicalmaterialstorage");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//Nineteenth checkbox group with name of biologicalmaterialstorage
function selectOnlyThis18(id) {
    var myCheckbox = document.getElementsByName("biologicalmaterialstorage");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//twentieth checkbox group with name of shipmentjustification
function selectOnlyThis19(id) {
    var myCheckbox = document.getElementsByName("shipmentjustification");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//twentyfirst checkbox group with name of safetyissuesaddressing
function selectOnlyThis20(id) {
    var myCheckbox = document.getElementsByName("safetyissuesaddressing");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//twentysecond checkbox group with name of researchwithanimalsapproval
function selectOnlyThis21(id) {
    var myCheckbox = document.getElementsByName("researchwithanimalsapproval");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//twentythird checkbox group with name of ethicalconcernsaddressing
function selectOnlyThis22(id) {
    var myCheckbox = document.getElementsByName("ethicalconcernsaddressing");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//twentyfourth checkbox group with name of ethicalconcernstesting
function selectOnlyThis23(id) {
    var myCheckbox = document.getElementsByName("ethicalconcernstesting");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//twentyfifth checkbox group with name of risktopotentialstudy
function selectOnlyThis24(id) {
    var myCheckbox = document.getElementsByName("risktopotentialstudy");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//twentysixth checkbox group with name of datacollectionandstorageplans
function selectOnlyThis25(id) {
    var myCheckbox = document.getElementsByName("datacollectionandstorageplans");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//twentyseventh checkbox group with name of specimencollectionandstorageplans
function selectOnlyThis26(id) {
    var myCheckbox = document.getElementsByName("specimencollectionandstorageplans");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//twentyeighth checkbox group with name of evidenceofaccesstoinformation
function selectOnlyThis27(id) {
    var myCheckbox = document.getElementsByName("evidenceofaccesstoinformation");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//twentynineth checkbox group with name of safeguardingrightsprovision
function selectOnlyThis28(id) {
    var myCheckbox = document.getElementsByName("safeguardingrightsprovision");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//thirtieth checkbox group with name of provisionsforcompensation
function selectOnlyThis29(id) {
    var myCheckbox = document.getElementsByName("provisionsforcompensation");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//thirtyfirst checkbox group with name of evidenceofethicstraining
function selectOnlyThis30(id) {
    var myCheckbox = document.getElementsByName("evidenceofethicstraining");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//thirtysecond checkbox group with name of reviewersrecomendation
function selectOnlyThis31(id) {
    var myCheckbox = document.getElementsByName("reviewersrecomendation");
    Array.prototype.forEach.call(myCheckbox, function (el) {
        el.checked = false;
    });
    id.checked = true;
}
//function to open page one
function openPageOne() {
    document.getElementById('page1').style.display = "block"; //make page one visible
    document.getElementById('page2').style.display = "none"; //make page two invisible
    document.getElementById('page3').style.display = "none"; //ensure page three is not displayed
}
//function to open page two
function openPageTwo() {
    document.getElementById('page2').style.display = "block"; //set page two's dispalay style property to block so that it is visible
    document.getElementById('page1').style.display = "none"; // make page one invisible
    document.getElementById('page3').style.display = "none"; // make sure that the display property of page three is set to none. this is important since we shall call this function later when the display property of page three has changed
}
//function to open page three
function openPageThree() {
    document.getElementById('page3').style.display = "block"; //make page three visible
    document.getElementById('page1').style.display = "none"; // still retain the invinsible property of page one
    document.getElementById('page2').style.display = "none"; //ensure page two is not displayed
}
//function to add callender to the date field
$(document).ready(function () {
    var date_input = $('input[name="date"]');
    var container = $('.bootstrap-iso form').length > 0 ? $('.bootstrap-iso form').parent() : "body";
    date_input.datepicker({
        format: 'mm/dd/yyyy',
        container: container,
        todayHighlight: true,
        autoclose: true
    });
});
//font changes for table headers and documents
$(document).ready(function () {
    //function to increase fonts
    $('#incfont').click(function () {
        curentBodySize = parseInt($('td, th').css('font-size')) + 2;
        if (curentBodySize <= 22)
            $('td, th').css('font-size', curentBodySize);
    });
    //function to decrease fonts
    $('#decfont').click(function () {
        curentBodySize = parseInt($('td, th, h1, h2, h3').css('font-size')) - 2;
        if (curentBodySize >= 10)
            $('td, th, h1, h2, h3').css('font-size', curentBodySize);
    });
});

               